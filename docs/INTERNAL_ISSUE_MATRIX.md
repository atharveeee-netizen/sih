# BEEVIL KNIEVEL — MASTER REPAIR LOOP ISSUE MATRIX

## Baseline Status
- **Commit SHA**: `bb24d19f9a60d80a10ce3e09ecc6ce0286edaeb5`
- **Primary Standard**: CODE → CONFIG → EXPERIMENT → MEASUREMENT → VALIDATION → DOCUMENTATION

---

## Internal Issue Matrix

| ID | Severity | Component / File | Problem Description | Evidence | Required Fix | Test / Validation | Status |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **ISS-01** | **P0** | `TinyML Model/bee_acoustic_classifier.py` | Labeled as "Quantized 1D-CNN", but is an `if/elif` rule engine with hardcoded confidence values (`0.99, 0.97, 0.95`). | `bee_acoustic_classifier.py` L1-6, L82-104 | Honestly rename to `Multi-Band Acoustic Spectral Feature Classifier`; replace fake confidence with `decision_score` / deterministic state; document actual DSP + threshold logic. | `run_stress_test_benchmark.py` (30/30 PASS) | 🟢 RESOLVED |
| **ISS-02** | **P0** | `Cloud Model/cloud_server.py` | Server does not load `cloud_advisor_model.joblib`; uses hardcoded `if/elif` thresholds with fake confidence (`96.5, 94.2, 91.8, 99.1`). | `cloud_server.py` L34-46 | Load `cloud_advisor_model.joblib` via `joblib.load()`; compute calibrated class probabilities (`clf.predict_proba()`); return real confidence or `decision_score`. | `run_cloud_model_benchmark.py` (4/4 PASS), `tests/test_cloud_model.py` (3/3 PASS) | 🟢 RESOLVED |
| **ISS-03** | **P0** | `Cloud Model/cloud_server.py` | Silently substitutes fake healthy defaults (`temp=34.5, audio=150, co2=800, weight=25`) when telemetry fields are missing. | `cloud_server.py` L75-78 | Remove silent fallbacks; validate incoming JSON; return `HTTP 422 Unprocessable Entity` on missing required telemetry. | `tests/test_cloud_model.py` assert status 422 on partial JSON | 🟢 RESOLVED |
| **ISS-04** | **P0** | `TinyML Model/run_full_zenodo_real_benchmark.py` | Benchmark masks classification failure (`is_correct` allows Queenless to predict Thermal Stress and still PASS); unconditional `Benchmark Status: PASSED`. | `run_full_zenodo_real_benchmark.py` L67-69, L92 | Separate scenario tolerance from exact classification evaluation; compute strict accuracy, precision, recall, F1, confusion matrix; exit non-zero on failure (`status = "PASSED" if passed == total else "FAILED"`). | `run_full_zenodo_real_benchmark.py` (12/12 PASS, 100% Acc/Prec/Rec/F1, CM calculated, exit code 0) | 🟢 RESOLVED |
| **ISS-05** | **P0** | `Cloud Model/build_cloud_model.py` | Claims "Sourcing academic distributions from HOBOS & BUT-2", but generates data via `np.random.choice` / `np.random.normal`. | `build_cloud_model.py` L22-52 | Label generated data honestly as `Synthetic Training Data (Parametric Empirical Distributions)`; document parameter provenance in metadata. | `Cloud Model/data/cloud_training_manifest.json` generated & verified | 🟢 RESOLVED |
| **ISS-06** | **P0** | `gateway/server.py` & `MODEL_REGISTRY.md` | `MODEL_REGISTRY.md` documents `beevil_fusion_net_edge_torchscript.pt` (18.9 MB PyTorch INT8 model), but binary does not exist on disk. | `MODEL_PATH.exists() == False` | Accurately register the Gateway Engine as a `Deterministic Multi-Modal Edge Expert Engine` with CUSUM detector; eliminate fictional PyTorch model claims. | `tests/test_full_gateway_pipeline.py` (6/6 PASS) | 🟢 RESOLVED |
| **ISS-07** | **P1** | `firmware/` (Scattered Magic Numbers) | Magic numbers for radio, sensors, algorithms, and power scattered across multiple headers without units or rationales. | `config.h`, `beevil_rak4631_transmitter.ino` | Centralize into `firmware/config/`: `hardware_config.h`, `radio_config.h`, `sensor_config.h`, `battery_config.h`, `algorithm_config.h`. | Updated `firmware/config/*.h` and included in transmitter & sensor nodes | 🟢 RESOLVED |
| **ISS-08** | **P1** | `docs/MODEL_REGISTRY.md` & Hardware Docs | Contradictory hardware references: mentions "Raspberry Pi CM4", "BCM2711", "DS18B20 Array" alongside "Raspberry Pi 3B+", "BCM2837B0", "TMP117". | `MODEL_REGISTRY.md` L11, L14 | Canonicalize to Raspberry Pi 3B+ (BCM2837B0) + Waveshare SX1262 LoRa HAT and 5x TI TMP117 array across all documents. | Authored `docs/HARDWARE_ARCHITECTURE.md` | 🟢 RESOLVED |
| **ISS-09** | **P1** | Documentation ("72-hour early warning") | Unqualified claim "predicts brood mortality 72 hours in advance" based on synthetic degradation. | `README.md`, `MODEL_REGISTRY.md` | Standardize wording: "The algorithm detects a modeled progressive thermal deficit with a 72-hour warning horizon in the evaluated synthetic scenario." | Text audit across `docs/`, `firmware/`, and `README.md` | 🟢 RESOLVED |
| **ISS-10** | **P1** | `docs/IEEE_HART_ANSYS_SIMULATION_DOSSIER.md` | Simulation results not clearly demarcated from laboratory measurements and field experimental data. | `IEEE_HART_ANSYS_SIMULATION_DOSSIER.md` | Apply Evidence Taxonomy (`[SIMULATED]`, `[CALCULATED]`, `[MEASURED]`, `[EXPERIMENTAL]`, `[TARGET]`) to every quantitative metric. | Tagged all quantitative sections in `IEEE_HART_ANSYS_SIMULATION_DOSSIER.md` | 🟢 RESOLVED |
| **ISS-11** | **P1** | Repository Hygiene & Duplicate Folders | `ANSYS SIMULATIONS/` duplicated from `simulations/`; `ml_models/` duplicated from `TinyML Model/`; empty `.gitmodules`; tracked `*.elf, *.hex` binaries. | Root directory inspection | Remove duplicate folders; delete empty `.gitmodules`; relocate or ignore firmware build artifacts; remove root scratch scripts (`app.js`, `team.html`). | Removed `ml_models/`, `.gitmodules`, moved firmware binaries to `artifacts/` | 🟢 RESOLVED |
| **ISS-12** | **P1** | Deployment Scripts (`setup_gateway.sh`) | Hardcoded `/home/pi` and username `pi`. | `gateway/setup_gateway.sh` L10-25 | Use configurable environment variables (`${GATEWAY_USER:-pi}`, `${GATEWAY_HOME:-/home/${GATEWAY_USER}}`). | Updated `setup_gateway.sh` and `DEPLOYMENT_GUIDE.md` | 🟢 RESOLVED |
| **ISS-13** | **P1** | Test Suite Discovery | `tests/test_full_gateway_pipeline.py` uses `run_pipeline_verification()` instead of `test_*`, causing `pytest` to collect 0 items. | `pytest tests/` output | Refactor into standard pytest test cases (`test_root()`, `test_hives()`, `test_telemetry()`, etc.). | `pytest tests/` collects and passes 9 items | 🟢 RESOLVED |
| **ISS-14** | **P1** | Missing Reproducibility & Limitations Docs | Lacks standalone `REPRODUCIBILITY.md` and `LIMITATIONS.md` required for hostile peer review. | Root directory inspection | Author `REPRODUCIBILITY.md` (exact commands to replicate all metrics) and `LIMITATIONS.md` (honest technical constraints). | Authored `REPRODUCIBILITY.md` and `LIMITATIONS.md` | 🟢 RESOLVED |
| **ISS-15** | **P1** | CI Workflow (`.github/workflows/ci.yml`) | Current CI only tests frontend deployment; lacks python test validation, benchmark execution, and hygiene checks. | `.github/workflows/` inspection | Build comprehensive GitHub Actions CI testing frontend, python tests, model validation, and hygiene. | Authored `.github/workflows/ci.yml` | 🟢 RESOLVED |
| **ISS-16** | **P2** | Pydantic & FastAPI Deprecations | Deprecation warnings on `min_items`/`max_items` and `@app.on_event("startup")` in `gateway/server.py`. | Server startup logs | Update Pydantic schemas to `min_length`/`max_length` and FastAPI lifespan handler. | Clean startup without deprecation warnings | 🟢 RESOLVED |
